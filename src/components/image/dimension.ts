export type Dimension = {
  mobile: {
    height: string,
    width: string,
    minHeight?: string,
    minWidth?: string,
    margin?: string,
    borderRadius?: string
  },
  desktop: {
    height: string,
    width: string,
    minHeight?: string,
    minWidth?: string,
    margin?: string,
    borderRadius?: string
  }
}